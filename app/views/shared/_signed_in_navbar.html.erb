<header nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to user_posts_path(current_user.id) do %>
          <%= image_tag 'danish_flag.gif' %>
      <% end %>

      <div class="row-fluid">
        <h1 class="col-md-3 col-md-offset-1">
          <%= link_to "Danebook", user_posts_path(current_user.id) %>
        </h1>
        <div id="navbar-collapse" class="collapse navbar-collapse">
        <h3>Signed in as
          <%= link_to current_user.first_name, current_user %>
          <%= link_to "Logout", logout_path, :method => :delete %></h3>
        </div>
      </div>
    </div>
  </div>
</div>
</header>
